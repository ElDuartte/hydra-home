# Triage Labels

For a solo project, we use two labels:

| Label | Meaning |
|-------|---------|
| `ready-to-build` | Clear to implement; next in queue |
| `wontfix` | Explicitly rejected; will not implement |

No other labels are needed. Issues start untagged (implicit backlog) until either label is applied.
